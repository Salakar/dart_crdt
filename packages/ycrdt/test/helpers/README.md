# Test Helpers

Place shared test utilities, mocks, deterministic random harnesses, and fixture
loaders in this directory. Keep feature-specific assertions close to their
tests; use helpers here only when multiple test files need the same setup.

Fixtures belong in `../fixtures/`. Golden image baselines belong in
`../goldens/`.
